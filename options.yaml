targets:
  - portainer
  - docker_socket_mount
  - docker_user_2_root

installs:
  - docker

tools:
  - harpoon

env:
  docker:
    DOCKER_VER: "5:18.09.1~3-0~debian-stretch" 
    SOME_TEST_ENV: hello